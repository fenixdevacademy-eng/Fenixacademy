# Configurações do npm para Vercel
engine-strict=true
legacy-peer-deps=false
fund=false
audit=false

# Resolver problemas de dependências específicas de plataforma
target_platform=linux
target_arch=x64
target_libc=glibc
