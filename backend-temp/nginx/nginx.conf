# 🌐 CONFIGURAÇÃO NGINX PARA FENIX ACADEMY API
# 
# Configuração otimizada para produção com:
# - Proxy reverso para a API
# - Load balancing
# - SSL/TLS
# - Compressão e cache
# - Segurança avançada

# Configuração principal
user nginx;
worker_processes auto;
worker_cpu_affinity auto;
worker_rlimit_nofile 65535;

# Configurações de eventos
events {
    worker_connections 1024;
    use epoll;
    multi_accept on;
}

# Configurações de HTTP
http {
    # Configurações básicas
    include /etc/nginx/mime.types;
    default_type application/octet-stream;
    
    # Configurações de performance
    sendfile on;
    tcp_nopush on;
    tcp_nodelay on;
    keepalive_timeout 65;
    keepalive_requests 100;
    
    # Configurações de buffer
    client_body_buffer_size 128k;
    client_max_body_size 50M;
    client_header_buffer_size 1k;
    large_client_header_buffers 4 4k;
    
    # Configurações de timeout
    client_body_timeout 12;
    client_header_timeout 12;
    send_timeout 10;
    
    # Configurações de compressão
    gzip on;
    gzip_vary on;
    gzip_min_length 1024;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types
        text/plain
        text/css
        text/xml
        text/javascript
        application/json
        application/javascript
        application/xml+rss
        application/atom+xml
        image/svg+xml;
    
    # Configurações de cache
    proxy_cache_path /var/cache/nginx levels=1:2 keys_zone=fenix_cache:10m max_size=10g inactive=60m use_temp_path=off;
    proxy_cache_key "$scheme$request_method$host$request_uri";
    proxy_cache_valid 200 60m;
    proxy_cache_valid 404 1m;
    
    # Configurações de rate limiting
    limit_req_zone $binary_remote_addr zone=api:10m rate=10r/s;
    limit_req_zone $binary_remote_addr zone=login:10m rate=1r/s;
    
    # Configurações de upstream para load balancing
    upstream fenix_api {
        least_conn;
        server api:5000 max_fails=3 fail_timeout=30s;
        server api:5000 max_fails=3 fail_timeout=30s;
        server api:5000 max_fails=3 fail_timeout=30s;
        keepalive 32;
    }
    
    # Configurações de upstream para WebSocket
    upstream fenix_websocket {
        ip_hash;
        server api:5000;
    }
    
    # Configurações de log
    log_format main '$remote_addr - $remote_user [$time_local] "$request" '
                    '$status $body_bytes_sent "$http_referer" '
                    '"$http_user_agent" "$http_x_forwarded_for" '
                    'rt=$request_time uct="$upstream_connect_time" '
                    'uht="$upstream_header_time" urt="$upstream_response_time"';
    
    access_log /var/log/nginx/access.log main;
    error_log /var/log/nginx/error.log warn;
    
    # Configurações de segurança
    server_tokens off;
    add_header X-Frame-Options DENY;
    add_header X-Content-Type-Options nosniff;
    add_header X-XSS-Protection "1; mode=block";
    add_header Referrer-Policy "strict-origin-when-cross-origin";
    add_header Content-Security-Policy "default-src 'self'; script-src 'self' 'unsafe-inline' 'unsafe-eval' https://js.stripe.com; style-src 'self' 'unsafe-inline' https://fonts.googleapis.com; font-src 'self' https://fonts.gstatic.com; img-src 'self' data: https:; connect-src 'self' https://api.stripe.com; frame-src 'self' https://js.stripe.com;";
    
    # Configurações de SSL
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers ECDHE-RSA-AES128-GCM-SHA256:ECDHE-RSA-AES256-GCM-SHA384:ECDHE-RSA-AES128-SHA256:ECDHE-RSA-AES256-SHA384;
    ssl_prefer_server_ciphers off;
    ssl_session_cache shared:SSL:10m;
    ssl_session_timeout 10m;
    
    # Configurações de proxy
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection 'upgrade';
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_cache_bypass $http_upgrade;
    proxy_buffering on;
    proxy_buffer_size 4k;
    proxy_buffers 8 4k;
    
    # Servidor HTTP (redireciona para HTTPS)
    server {
        listen 80;
        server_name fenixacademy.com www.fenixacademy.com;
        
        # Redireciona para HTTPS
        return 301 https://$server_name$request_uri;
    }
    
    # Servidor HTTPS principal
    server {
        listen 443 ssl http2;
        server_name fenixacademy.com www.fenixacademy.com;
        
        # Configurações SSL
        ssl_certificate /etc/nginx/ssl/fenixacademy.com.crt;
        ssl_certificate_key /etc/nginx/ssl/fenixacademy.com.key;
        
        # Configurações de segurança adicional
        ssl_stapling on;
        ssl_stapling_verify on;
        resolver 8.8.8.8 8.8.4.4 valid=300s;
        resolver_timeout 5s;
        
        # Configurações de HSTS
        add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;
        
        # Configurações de cache para arquivos estáticos
        location ~* \.(jpg|jpeg|png|gif|ico|css|js|pdf|txt|woff|woff2|ttf|eot)$ {
            expires 1y;
            add_header Cache-Control "public, immutable";
            access_log off;
        }
        
        # Configurações para uploads
        location /uploads/ {
            alias /var/www/uploads/;
            expires 1y;
            add_header Cache-Control "public";
            add_header X-Content-Type-Options nosniff;
        }
        
        # Configurações para API
        location /api/ {
            # Rate limiting
            limit_req zone=api burst=20 nodelay;
            
            # Proxy para a API
            proxy_pass http://fenix_api;
            proxy_cache fenix_cache;
            proxy_cache_use_stale error timeout updating http_500 http_502 http_503 http_504;
            proxy_cache_valid 200 60m;
            proxy_cache_valid 404 1m;
            
            # Headers de cache
            add_header X-Cache-Status $upstream_cache_status;
            
            # Timeouts
            proxy_connect_timeout 30s;
            proxy_send_timeout 30s;
            proxy_read_timeout 30s;
        }
        
        # Configurações para WebSocket
        location /socket.io/ {
            proxy_pass http://fenix_websocket;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            
            # Timeouts para WebSocket
            proxy_connect_timeout 7d;
            proxy_send_timeout 7d;
            proxy_read_timeout 7d;
        }
        
        # Configurações para autenticação
        location /api/auth/login {
            limit_req zone=login burst=5 nodelay;
            proxy_pass http://fenix_api;
        }
        
        location /api/auth/register {
            limit_req zone=login burst=3 nodelay;
            proxy_pass http://fenix_api;
        }
        
        # Configurações para health check
        location /health {
            access_log off;
            proxy_pass http://fenix_api;
        }
        
        # Configurações para status da API
        location /api/status {
            access_log off;
            proxy_pass http://fenix_api;
        }
        
        # Configurações para documentação
        location /api-docs {
            proxy_pass http://fenix_api;
            proxy_cache off;
        }
        
        # Configurações para admin
        location /api/admin/ {
            # Rate limiting mais restritivo para admin
            limit_req zone=api burst=10 nodelay;
            
            proxy_pass http://fenix_api;
            proxy_cache off;
        }
        
        # Configurações para webhooks
        location /api/webhooks/ {
            # Sem rate limiting para webhooks
            proxy_pass http://fenix_api;
            proxy_cache off;
        }
        
        # Configurações para notificações
        location /api/notifications/ {
            proxy_pass http://fenix_api;
            proxy_cache off;
        }
        
        # Configurações para analytics
        location /api/analytics/ {
            proxy_pass http://fenix_api;
            proxy_cache off;
        }
        
        # Configurações para pagamentos
        location /api/payments/ {
            proxy_pass http://fenix_api;
            proxy_cache off;
        }
        
        # Configurações para cursos
        location /api/courses/ {
            proxy_pass http://fenix_api;
            proxy_cache fenix_cache;
            proxy_cache_valid 200 30m;
        }
        
        # Configurações para aulas
        location /api/lessons/ {
            proxy_pass http://fenix_api;
            proxy_cache fenix_cache;
            proxy_cache_valid 200 30m;
        }
        
        # Configurações para usuários
        location /api/users/ {
            proxy_pass http://fenix_api;
            proxy_cache off;
        }
        
        # Configurações para matrículas
        location /api/enrollments/ {
            proxy_pass http://fenix_api;
            proxy_cache off;
        }
        
        # Página de erro personalizada
        error_page 404 /404.html;
        error_page 500 502 503 504 /50x.html;
        
        location = /404.html {
            root /var/www/html;
            internal;
        }
        
        location = /50x.html {
            root /var/www/html;
            internal;
        }
        
        # Configurações de segurança para arquivos sensíveis
        location ~ /\. {
            deny all;
            access_log off;
            log_not_found off;
        }
        
        location ~ ~$ {
            deny all;
            access_log off;
            log_not_found off;
        }
    }
    
    # Servidor para desenvolvimento local
    server {
        listen 80;
        server_name localhost;
        
        # Configurações para desenvolvimento
        location / {
            proxy_pass http://fenix_api;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }
        
        # Configurações para WebSocket em desenvolvimento
        location /socket.io/ {
            proxy_pass http://fenix_websocket;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
            proxy_set_header Host $host;
        }
    }
}



